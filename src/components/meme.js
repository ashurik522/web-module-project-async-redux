import React from "react";
import { connect } from "react-redux";

const Meme = (props) => {
    
    const {meme} = props;

    return(
        <div>
            Get map working
        </div>
    )
}


export default Meme;